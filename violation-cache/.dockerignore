# Exclude everything that is not needed inside the container image.
# server.js plus db/, lib/ and routes/ are COPYed, so this is a safety net.
node_modules
npm-debug.log
package-lock.json
*.test.js
*.md
.env
data/
pgdata/
