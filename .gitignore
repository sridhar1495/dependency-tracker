.claude/settings.local.json
node_modules/

# Runtime secrets and data — never commit these.
# Neither .env nor the runtime data directory was previously ignored, so a live
# DT API key or SMTP password could be committed by accident (CLAUDE.md §15).
.env
violation-cache/data/
violation-cache/pgdata/
